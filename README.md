# Bom Prédio — MVP v2

Atualização: adicionado React Router e páginas de Marketplace (Home), proteção de rota do Dashboard e componentes principais.

Rodar:
1. npm install
2. npm run dev

Variáveis de ambiente: copie .env.example para .env com VITE_SUPABASE_URL e VITE_SUPABASE_ANON_KEY
